# Hosted coach

This directory is where a later "ask the coach" call will live. It is off.

Live posture stays on the machine that has the camera. A hosted model, when you connect one, should receive a still image or the keypoints only after you press a button. It should not receive a continuous video stream.

Weights and API tokens do not belong in this repository.
